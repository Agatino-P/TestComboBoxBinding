<Window x:Class="TestComboBoxBinding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TestComboBoxBinding"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow" Width="800" Height="450"
        d:DataContext="{d:DesignData Type=local:MainWindow,
                                     IsDesignTimeCreatable=True}"
        mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="97*" />
            <ColumnDefinition Width="101*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid x:Name="grid1" DataContext="{Binding BoundReader}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Margin="3"
                   HorizontalAlignment="Left" VerticalAlignment="Center"
                   Content="Name:" />
            <TextBox x:Name="nameTextBox" Grid.Row="0" Grid.Column="1"
                     Width="120" Height="23" Margin="3"
                     HorizontalAlignment="Left" VerticalAlignment="Center"
                     Text="{Binding Name, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" />
            <Label Grid.Row="1" Grid.Column="0" Margin="3"
                   HorizontalAlignment="Left" VerticalAlignment="Center"
                   Content="Book Id:" />
            <TextBox x:Name="bookIdTextBox" Grid.Row="1" Grid.Column="1"
                     Width="120" Height="23" Margin="3"
                     HorizontalAlignment="Left" VerticalAlignment="Center"
                     Text="{Binding BookId, Mode=TwoWay, NotifyOnValidationError=true, ValidatesOnExceptions=true}" />
        </Grid>
        <DataGrid x:Name="bookDataGrid" Grid.Column="1"
                  AutoGenerateColumns="False" EnableRowVirtualization="True"
                  ItemsSource="{Binding Books}"
                  RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="idColumn" Width="SizeToHeader"
                                    Binding="{Binding Id}"
                                    Header="Id" />
                <DataGridTextColumn x:Name="titleColumn" Width="SizeToHeader"
                                    Binding="{Binding Title}"
                                    Header="Title" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
